## FastAPI CRUD app with async DB and SQLAlchemy

## Run app
```bash
docker-compose -f docker-compose.app.yml up
```
## Run tests
```bash
docker-compose -f docker-compose.test.yml run web pytest -vv
```